and-activity-lifecycle
----------------------

Demonstrates the "onX" lifecycle related 
methods of an activity and when they are invoked. 

Run this sample and then send other events to the 
device/emulator (incoming call, kill VM, etc). 

See "outcome.txt" for an example. 